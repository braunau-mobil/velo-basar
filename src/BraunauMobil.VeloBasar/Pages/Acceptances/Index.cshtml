@page
@model BraunauMobil.VeloBasar.Pages.Acceptances.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Acceptance[0].Seller)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Acceptance[0].Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Acceptance[0].TimeStamp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Acceptance[0].Basar)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Acceptance) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Seller.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeStamp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Basar.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
