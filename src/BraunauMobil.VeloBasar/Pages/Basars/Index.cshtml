@page
@model BraunauMobil.VeloBasar.Pages.Basars.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Basar[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Basar[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Basar[0].ProductCommission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Basar[0].ProductDiscount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Basar[0].SellerDiscount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Basar) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductCommission)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductDiscount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SellerDiscount)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
